<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin jaringan untuk WebView portal -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Feedback getar saat ada peringatan -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
        Izin normal untuk blokir notifikasi (Do Not Disturb).
        Bukan izin sensitif - tidak memicu Google Play Protect.
        Dipakai agar notifikasi tidak muncul saat ujian berlangsung.
    -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Exambrowser"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <!--
            Aktivitas utama: WebView kiosk ujian.
            - exported=true karena menjadi launcher.
            - FLAG_SECURE + immersive + Lock Task Mode ditangani di dalam kode.
            - lockTaskMode="if_whitelisted" mendukung mode kiosk via Screen Pinning.
        -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard|smallestScreenSize|density|uiMode"
            android:enableOnBackInvokedCallback="false"
            android:exported="true"
            android:launchMode="singleTask"
            android:lockTaskMode="if_whitelisted"
            android:theme="@style/Theme.Exambrowser"
            android:taskAffinity="com.thhk.exambrowser.main"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>